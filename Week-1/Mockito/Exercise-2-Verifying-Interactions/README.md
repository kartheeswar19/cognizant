# Exercise 2: Verifying Interactions

This exercise demonstrates how to verify that a method called on a mocked dependency was actually invoked.

- Interaction verification: Checking whether a mocked method was called.
- verify(): Mockito's method for asserting interactions.
- Why this is useful: It confirms that your code triggers the expected dependency calls.
