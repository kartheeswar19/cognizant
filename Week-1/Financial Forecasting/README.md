Recursion simplifies forecasting calculations by repeatedly applying the growth rate.
Recursive forecasting has O(n) time complexity and O(n) space complexity.
An iterative solution is more efficient because it uses O(1) extra space and avoids stack overflow for large forecasting periods.
